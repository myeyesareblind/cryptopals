(ns cryptopals.core
  (:require [cryptopals.str-vec-utils :as svu])
  (:import [org.apache.commons.codec.binary.Base64]
           [org.apache.commons.codec.binary.StringUtils])
  (:gen-class))

(defn -main
  "I don't do a whole lot ... yet."
  [& args]
  (println (svu/encode "1231231dfs"))
  (println "Hello, World!"))
